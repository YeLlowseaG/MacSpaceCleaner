# Mac 空间清理

## 项目位置

项目根目录：

`/Volumes/MacT7/projectDocuments/MacSpaceCleaner`

## 主要文件

- `MacSpaceCleaner.swift`：App 主程序源码
- `Info.plist`：App 名称、版本和系统要求
- `IconGenerator.swift`：应用图标生成器
- `release.sh`：构建通用架构并进行 Developer ID 签名
- `notarize.sh`：提交 Apple 公证、装订票据并生成校验文件
- `dist/`：构建和公证后的发布产物
- `release-package/`：可以直接上传到官网的 DMG 和 SHA-256 文件
- `docs/`：使用说明文档
- `website/`：官网源码、产品截图、隐私政策、使用帮助和下载文件

## GitHub 与 Vercel

项目根目录已整理为 Git 仓库结构。Vercel 导入 GitHub 仓库后，将 Root Directory 设置为 `website`，即可部署静态官网；详细设置见根目录 `README.md`。

## 当前发布版本

- 版本：1.3（Build 5）
- 支持：Apple Silicon 和 Intel Mac
- 最低系统：macOS 14
- Bundle ID：`com.yellow.macspacecleaner`
- 签名：Developer ID Application
- 状态：已通过 Apple 公证并装订票据

## 重新构建

在“终端”中执行：

```bash
cd "/Volumes/MacT7/projectDocuments/MacSpaceCleaner"
./release.sh
```

## 重新公证

构建成功后执行：

```bash
cd "/Volumes/MacT7/projectDocuments/MacSpaceCleaner"
./notarize.sh
```

公证凭据保存在当前 Mac 的钥匙串中，配置名称为 `MacSpaceCleanerNotary`。

## 注意

构建或发布期间不要拔出 MacT7。`release-package/MacSpaceCleaner-1.3.dmg` 是当前可对外发布的安装包。
